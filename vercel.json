{
  "buildCommand": "cd app/linyappcraft && npm install --legacy-peer-deps && npx vite build",
  "outputDirectory": "app/linyappcraft/dist",
  "framework": null,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
